# Virtual environments — do not commit (recreate with python -m venv .venv && pip install -r requirements.txt)
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/

# OS
.DS_Store

# ECS170: pickled / binary prediction outputs from scripts (regenerate by running scripts)
ECS170_Spring_2026_Source_Code/result/stage_*_result/*

# Local planning note
ECS170_Spring_2026_Source_Code/README_STAGE_2_PLAN.md

# Stage 2 datasets — too large for GitHub (train.csv > 100MB). Keep locally / from instructor.
ECS170_Spring_2026_Source_Code/data/stage_2_data/*.csv
